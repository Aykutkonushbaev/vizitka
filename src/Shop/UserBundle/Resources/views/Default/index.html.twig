<html>
    <head>
        <link rel="stylesheet" href="{{ asset('public/style.css') }}" >
        <link rel="stylesheet" href="{{ asset('public/bootstrap.min.css') }}">
    </head>
    <body>
    <div class="container">
        <div class="row">
            <h3>КошууОкууЖанылооОчуруу</h3>
            <h3>CreateReadUpdateDelete</h3>
        </div>
        <div class="row">
            <p>
                <a class="btn btn-success" href="{{path ('shop_user_create') }}">Кошуу</a>
            </p>
        <table class="table table-stripped table-bordered ">
            <thead>
            <tr>
                <td>
                    <h6 class="legend">ID</h6>
                </td>
                <td>
                    <h6 class="legend">Name</h6>
                </td>
                <td>
                    <h6 class="legend">Surname</h6>
                </td>
                <td>
                    <h6 class="legend">Age</h6>
                </td>
                <td>
                    <h6 class="legend">Action</h6>
                </td>
            </tr>
            </thead>
            <tbody>
                {% for user in users %}
                <tr>
                    <td>
                        <p> {{ user.getId() }}  </p>
                    </td>
                    <td>
                        <p> {{ user.getName() }}  </p>
                    </td>
                    <td>
                        <p> {{ user.getLastname() }} </p>
                    </td>
                    <td>
                        <p> {{ user.getAge() }} </p>
                    </td>
                    <td>
                        <a class="btn btn-danger" href="{{ path('shop_user_delete', {'user':user.id} ) }}"> Delete </a>
                        <a class="btn btn-info" href="{{ path('shop_user_read', {'user':user.id} ) }}"> Read </a>
                        <a class="btn btn-warning" href="{{ path('shop_user_update', {'user':user.id} ) }}"> Update </a>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        </div>
    </div>
    </body>
</html>
